strArr = ["and", "notad", "abcd"];

function deletePartOfArry(strArr) {
  var answer = [];
  if (strArr.includes("ad")) {
  }
  console.log(answer);

  return answer;
}

deletePartOfArry(strArr);

console.log(strArr.includes("ad"));
